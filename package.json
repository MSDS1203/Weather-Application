{
  "name": "weather-application",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd backEnd && node server.js",
    "client": "cd mapandmore && npm start"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}